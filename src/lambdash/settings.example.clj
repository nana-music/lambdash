(ns lambdash.settings)

(def database "FIXME: Use Database")

(def redis
  {:pool {} :host "localhost" :port 6379})

(def auth
  {:username "FIXME: Username"
   :password "FIXME: Password"})

(def mongodb
  {:host "FIXME: use mongodb"})
